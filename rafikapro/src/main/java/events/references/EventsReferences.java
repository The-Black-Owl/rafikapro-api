package events.references;

public class EventsReferences {
    public enum EventCategory{
        CONFERENCE, SPORT, THEATRE, CONCERT, FESTIVAL
    }

    public enum EventStatus{
        PLANNED, ON_SALE, SOLD_OUT, CANCELLED
    }
}
